 * url for data fetching should be configurable. `fetch` no longer hardcodes the download
   url ( it resolves it from the data.gov.tw dataset api ), but the dataset ids are still
   written into the script.
